FROM node:18-alpine

WORKDIR /app

# Copy built Next.js application (we'll build locally first)
COPY .next/standalone ./
COPY .next/static ./.next/static
COPY public ./public

EXPOSE 3000

ENV NODE_ENV=production
ENV PORT=3000

CMD ["node", "server.js"]